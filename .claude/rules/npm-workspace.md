---
paths:
  - "package.json"
  - "package-lock.json"
  - "**/package.json"
---

# npm у `D:\QA` — перед будь-якою командою перевірити `package.json`

Файл повернуто в робоче дерево 14.09.2026. Якщо він знову зникне, `npm install`
і `npm i <пакет>` у корені репо вичистять `node_modules` як «зайвий» —
так уже було зламано `@playwright/test`.

Тому порядок такий:

```bash
test -f package.json || git checkout -- package.json   # спершу переконатись, що файл є
```

і аж тоді будь-яка npm-команда.

**Версії зафіксовані:** `playwright`, `playwright-core`, `@playwright/test` — усі `1.57.0`.
Не піднімати без потреби: під новіші потрібні інші збірки браузерів, а вся
крос-браузерна матриця в `qa-toolkit/` розрахована саме на цю версію.

**Нових npm-залежностей у qa-toolkit не додавати** — набір свідомо побудований
на тому, що вже є в репо.
